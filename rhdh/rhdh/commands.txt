Course: Red Hat Learning Portal
Building Internal Developer Platform (IDP) with Red Hat Developer Hub

oc rollout restart deployment/my-deployment
oc rollout restart statefulset/my-statefulset
oc rollout restart deployment -n my-namespace


oc -n demo-project get secret rhdh-k8s-sa-token -o jsonpath="{.data.token}" | base64 -d

Module 2

the following are invoked from created artifacts in users github -- spring-boot-reactjs-app-1
argocd app create -f springboot_image.yaml
argocd app create -f springboot_app.yaml 

Module 3
from user copy of course repo
https://github.com/dbrugger946/rhdp-idp

git checkout -b phase-3-work phase-3-start
git merge phase-2-work 

might need to uncommit due to secrets issues
git reset origin
and just commit working files

oc edit argocd openshift-gitops -n openshift-gitops
remove -PipelineRun under resourceExclusions

oc get configmap argocd-cm -n openshift-gitops -o yaml
to confirm

oc rollout restart deployment openshift-gitops-server -n openshift-gitops

argocd app list

argocd app delete openshift-gitops/springboot-image-build --cascade --yes

argocd app delete openshift-gitops/springboot-app --cascade --yes

using user goldenpath approach
https://github.com/dbrugger946/RHCourse_RHDH_Golden_Path
use branch module3


